# Retrospective — Epic 1: Join astromatch and complete my intentional profile

**Project:** astromatch  
**Date:** 2026-03-28  
**Facilitator:** Bob (Scrum Master)  
**Participants:** Oliver (Project Lead), Alice (Product Owner), Charlie (Senior Dev), Dana (QA), Elena (Junior Dev)

## Epic scope

**Stories completed:** 12/12 (all marked `done` in `sprint-status.yaml`)

Stories 1.1–1.12: mobile/API scaffold, auth, recovery, age/deletion, consents, birth context, location, dynamics, photos/bio, auth rate limits, onboarding shell.

## Facilitation excerpt (party mode)

Bob (Scrum Master): "Epic 1 set the foundation—Expo, Spring Boot, Flyway, JWT envelopes, OpenAPI. Oliver, what felt strongest about how we landed this slice?"

Oliver (Project Lead): "Having a single contract file and CI-friendly API tests early reduced thrash later."

Alice (Product Owner): "We got from zero to a credible onboarding path without exposing engine scores—that matched the PRD."

Charlie (Senior Dev): "The vertical slices (register → profile fields) beat a big-bang schema dump. Flyway migrations stayed reviewable."

## What went well (systems & process)

- **Greenfield alignment:** Repo layout (`astromatch-mobile`, `astromatch-api`, `api-contract`) matches architecture notes; onboarding flows map to UX-DR11-style journey.
- **Safety-by-default:** Minimum age, consents, and account deletion landed with server enforcement, not UI-only.
- **Testability:** Health checks and integration-style tests established a pattern for later epics.

## Challenges & growth areas (no blame)

- **Surface area:** Twelve stories in one epic is a lot to coordinate; dependency clarity (what blocks “complete onboarding”) matters for demos.
- **TBDs in PRD:** Several “TBD” items (exact auth methods, limits) required sensible defaults—document decisions in story files when product is silent.

## Insights

- Establishing **JSON envelopes + traceId** and **JWT** early paid off for every subsequent epic.
- **Rate limits on auth** (Story 1.11) set a template reused for feed and safety endpoints later.

## Continuity — previous epic

None (first epic).

## Preparation for Epic 2

- Feed depends on **completed onboarding** and **profile photos** paths from Epic 1—verify photo URLs and auth headers in mobile client are stable.
- **Opaque ordering** for candidates is a product commitment; keep engine details server-side.

## Action items

| # | Action | Owner | Success criteria |
|---|--------|--------|------------------|
| A1 | Keep `openapi.yaml` version bumped when identity/profile paths change | Dev | CI contract check passes; mobile types stay aligned |
| A2 | Maintain a short “defaults & TBD” note for PRD gaps touched in Epic 1 | PO | New engineers can onboard without guessing policy |

## Readiness note

Epic 1 stories are complete in tracking; production hardening (secrets, TLS, store policies) remains a cross-cutting backlog outside this retro.

## Significant discoveries for roadmap

None that invalidate Epic 2 planning; entitlement **stub vs real** was flagged in `epics.md` final validation—track when Epic 5 integrates.

---

_Retrospective facilitated per BMad `bmad-retrospective` workflow. Psychological safety: focus on systems, not individuals._
